What is the purpose of Hashicorp configuration language (HCL)? 
- To define resoruces and configurations in a human readable format. Its defined for infra configuration

What is the default file created by Terraform to store the execution plan? 
- plan.out

What does the .terraform directory contain? 
- Provider plugins and metadata

Terraform fmt - reformts the .tf files

How to import an existing S3 bucket in terraform? 
- use the terraform import command with the resrouce and bucket name